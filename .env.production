# Production Environment Configuration
# This file is used for production builds (EAS builds, not Expo Go)

# Set environment to production
# This will automatically use the production backend URL
# and disable Expo Go mode
EXPO_PUBLIC_APP_ENV=production

# Ngrok URL is not used in production
# EXPO_PUBLIC_NGROK_URL=
